<?xml version="1.0" encoding="utf-8"?>
<paths>
    <external-path name="Camera" path="CameraN" />
    // path = /External Storage/CameraN이 된다.
    // name = content:// 로 시작하는 uri 주소체계의 suffix가 된다.
    //external-path name= 속성값에 대한 주소 path= 실제 물리 주소
</paths>